//
//  Theme.swift
//  BCBox
//
//  Created by Heidie Lee on 2025/5/26.
//

import SwiftUI

extension Color {
    // 使用綠色主題但改為淺色調
    static let appPrimary = Color(red: 76/255, green: 175/255, blue: 80/255) // 主要綠色
    static let lightBackground = Color(.systemBackground) // 白色背景
    static let cardBackground = Color(.systemGray6) // 淺灰色卡片背景
    static let textPrimary = Color(.label) // 主要文字
    static let textSecondary = Color(.secondaryLabel) // 次要文字
}

